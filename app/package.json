{
  "name": "word-castle",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "tsc -p tsconfig.test.json && node --test .test-build/",
    "data": "npm run test -- --test-name-pattern=__none__ && node scripts/convert-data.mjs",
    "check": "tsc -b && npm test"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/react": "18.3.28",
    "@types/react-dom": "18.3.7",
    "typescript": "5.9.3",
    "vite": "6.4.2"
  }
}
